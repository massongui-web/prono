import React from "react";
import ReactDOM from "react-dom/client";
import PronoChart from "./PronoChart.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PronoChart />
  </React.StrictMode>
);
